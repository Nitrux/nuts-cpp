# Library
add_subdirectory(lib)

# Agent (single-session chroot binary)
add_subdirectory(agent)

# Helper (privileged backend)
add_subdirectory(helper)

# GUI (MauiKit application)
add_subdirectory(gui)
